spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trainingdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  database-platform: org.hibernate.dialect.MySQL5Dialect
  show-sql: true

message: Have a great day - default